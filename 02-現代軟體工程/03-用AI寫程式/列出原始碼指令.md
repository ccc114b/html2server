如果你沒用 opencode / claudecode / cursor 這類的 IDE ，而是用網頁版，例如 gemini / aistudio

這些網頁版有些不支援你上傳程式類型的檔案，此時你可以用下列方式列出檔案後傳回

```
find . -type f \( -name "*.py" -o -name "*.html" \) -exec tail -n +1 {} +
```

上述指令會列出 *.py 和 *.html 的檔案
